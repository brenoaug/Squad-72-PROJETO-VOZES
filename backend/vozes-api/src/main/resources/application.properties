# Nome da aplicação
spring.application.name=vozes-api

# --- Configuração para o Banco de Dados do Docker (Desenvolvimento Local) ---
spring.datasource.url=jdbc:postgresql://database:5432/vozes
spring.datasource.username=vozesuser
spring.datasource.password=vozespass

# --- Configurações do Hibernate/JPA ---
# Dialeto para o PostgreSQL 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# 'update' é bom para desenvolvimento, para criar as tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update
# Boa prática para APIs, previne erros de LazyInitialization
spring.jpa.open-in-view=false

# --- Configuração do Servidor ---
# A porta padrão do Spring Boot é 8080. O Render fornecerá a porta dele via variável de ambiente.
server.port=${PORT:8080}

# --- Configuração de Segurança ---
# Sua chave secreta para os tokens JWT
api.security.token.secret=minha-chave-para-o-projeto-vozes

# --- Configuração de Paginação ---
spring.data.web.pageable.page-parameter=page
spring.data.web.pageable.size-parameter=size